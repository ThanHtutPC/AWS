---
plugin: aws_ec2
aws_access_key: "{{ access_key }}" 
aws_secret_key: "{{ secret_key }}" 
regions: 
  - us-east-1
#keyed_groups: 
#  - key: tags
#    prefix: tag
hostnames: 
  - ip-address 
groups: 
  redhat: "'RedHat' in tags.OS"
